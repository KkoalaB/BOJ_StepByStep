#include <iostream>
#include <utility>

using namespace std;

int main() {
    int N, x, y;
    pair<int, int> input[100000];

    scanf("%d", &N);

    for(int i = 0; i < N; i++) {
        scanf("%d%d", &x, &y);
        input[i] = make_pair(x, y);
    }

    for(int i = 0; i < N; i++) {
        
    }
}